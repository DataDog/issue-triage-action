---
# Caller workflow that exercises the reusable issue-triage workflow on THIS repo.
#
# NOTE: while PR #2 is not merged, this points at the PR branch
# (@nicolas.schweitzer/reusable) so we can test before merging. Once the PR is
# merged, switch the ref back to @main.
name: "Issue Triage (self-test)"

on:
  issues:
    types: [opened, reopened]
  workflow_dispatch:
    inputs:
      issue_number:
        description: 'Issue number to triage manually'
        required: true
        type: number

jobs:
  triage:
    uses: DataDog/issue-triage-action/.github/workflows/issue-triage.yml@nicolas.schweitzer/reusable
    with:
      slack_map_path: .github/workflows/config/github_slack_map.yaml
      # fallback_channel: agent-devx-ops   # optional, this is the default
      # fallback_team: agent-devx          # optional, this is the default
      # environment_name: main             # optional, this is the default
      issue_number: ${{ github.event_name == 'workflow_dispatch' && fromJSON(github.event.inputs.issue_number) || 0 }}
    secrets:
      ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
      SLACK_BOT_TOKEN:   ${{ secrets.SLACK_DATADOG_AGENT_BOT_TOKEN }}
