# release/

Release engineering: versioning, changelog generation, packaging (deb/rpm/msi),
signing, repository publishing, and release notes.

Common issues that belong here: broken installer, wrong package version,
missing artifact for a platform, repository/mirror problems, signature errors.

Owned by `@DataDog/agent-delivery`.
